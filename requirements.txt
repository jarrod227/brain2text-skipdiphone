# Install PyTorch with CUDA support first (before running pip install -r requirements.txt):
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# Adjust cu118 to match your CUDA version (cu121, cu124, etc.)
# See https://pytorch.org/get-started/locally/ for the right command.
torch>=2.1.0
numpy>=1.24.0
editdistance>=0.6.3
omegaconf>=2.3.0
tqdm>=4.65.0
pyyaml>=6.0
matplotlib>=3.7.0
pandas>=2.0.0
transformers>=4.40.0   # GPT-2 rescoring (rescore.py); install in lm_decode env
pytest>=7.0.0          # unit tests (tests/); dev-only
